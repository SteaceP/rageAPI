package types

type contextKey string

// Context keys
const (
	KeyUserID contextKey = "user_id"
	KeyDB     contextKey = "db"
)

// Constants
const (
	RoleAdmin string = "admin"
	IDField   string = "id"
	UserID    string = "user_id"
)
